# Prometheus 抓取配置（首次部署模板）
# 目标使用 Compose 服务名 + 容器端口 9090
# 不要写成宿主机映射端口（如 9191）

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: ikuai
    static_configs:
      - targets:
          - ikuai-exporter:9090
